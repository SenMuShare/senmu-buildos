# {{PROJECT_NAME}} 项目地图

> 文档状态：初始化草案  
> 权威项目根：`{{PROJECT_ROOT}}`  
> 项目形态：`{{PROJECT_TYPE}}`
> 生命周期意图：`{{LIFECYCLE_INTENT}}`
> 交付模式：`{{DELIVERY_MODEL}}`
> 组合方式：`{{COMPOSITION}}`
> 公开模型：`{{PUBLICATION_MODEL}}`
> 发布渠道：`{{RELEASE_CHANNELS}}`
> 已确认制品：`{{ARTIFACT_KINDS}}`
> 项目布局：`{{LAYOUT}}`
> 工作区根：`{{WORKSPACE_ROOT}}`
> 最近校准：{{DATE}}

本文件帮助新人和 AI 在不读取全项目的情况下找到正确责任主体。它只保存导航、归属和边界；详细规则链接到对应权威文档，不在这里复制。

当前任务及进度统一从 `governance/tasks/TASK_REGISTER.md` 进入，再读取对应 `TASK-<NNNN>-<slug>.md` 编号计划；本地图不保存可变任务状态。

## 物理目录与 Git 边界

| 标准角色 | 实际路径 | Git 跟踪 | 说明 |
| --- | --- | --- | --- |
| 项目系统 | `<项目根>` | `是` | 规则、代码、配置、schema、索引和小型证据 |
| 源输入 | `<路径或不适用>` | `否／按项目规则` | 原始或冻结输入 |
| 工作区 | `<路径或不适用>` | `否` | 中间文件、运行状态和可重建结果 |
| 最终交付 | `<路径或不适用>` | `否／按项目规则` | 已验收交付物 |
| 归档 | `<路径或不适用>` | `否` | 历史和关闭内容，不作为当前事实 |

表中实际路径默认使用相对工作区或相对权威根的路径。绝对路径只允许运行时解析或进入 Git 忽略的本机配置，不得成为项目身份、模板内容或公开发布事实。

## 责任与入口地图

先用本表确定唯一 owner，再进入对应目录。任何新文件、模块、流程或交付物都必须能在本表找到归属；无法归属时先修订本表，不得直接创建第二套目录或状态源。

| 领域/模块/流程 | 权威路径 | 单一责任 | 状态源/公开入口 | 交付或发布单元 | 变更前应读取 |
| --- | --- | --- | --- | --- | --- |
| `<名称>` | `<目录或文件>` | `<只写一个主要责任>` | `<状态文件、命令、API或入口>` | `<单元或不适用>` | `<权威文档>` |

## 项目规范索引

本表只帮助 AI 判断当前任务需要读取哪些项目规范；完整规则、理由、例外和命令保留在“权威路径”，不得复制到本表。先更新正式 owner，再同步本索引。同一 owner 可按不同任务信号登记多条入口，完全相同的行只保留一条。

active 行的项目内“权威路径”使用相对项目根的代码路径，或使用相对本文件的 Markdown 链接，使 validator 可以核验可达性；外部系统写可验证的公开入口。索引完整只表示已声明入口可达，不表示规则语义已由脚本证明，也不要求把项目所有 Markdown 收进来。

| 规范领域 | 适用信号 | 一句话决策摘要 | 权威路径 | 状态／最近校准 |
| --- | --- | --- | --- | --- |
| `<代码质量／架构／测试／工作流／交付等>` | `<什么任务或风险出现时读取>` | `<只写影响选择的短摘要>` | `<文档、配置或公开入口>` | `<active/candidate/legacy/retired；日期>` |

## 共享能力与禁止平行建设

| 能力 | 现有实现/组件/设计系统 | 允许扩展边界 | 禁止事项 |
| --- | --- | --- | --- |
| `<能力>` | `<路径或依赖>` | `<扩展点>` | `<不得另建的第二套实现>` |

## Legacy、临时区与迁移边界

| 路径/入口 | 状态 | 允许用途 | 替代项/退出条件 |
| --- | --- | --- | --- |
| `<路径>` | `<legacy/temporary/deprecated>` | `<只读、兼容、迁移等>` | `<权威替代项与移除条件>` |
