# TD-<NNNN>：<技术设计名称>

> Professional Name：Technical Design
> 文档状态：初始化草案
> Owner：`<待确认>`
> 适用范围／发布单元：`<待确认>`
> 最近校准：{{DATE}}
> 来源 Task：`<TASK-NNNN>`
> 技术评审：`<Engineering review owner 路径或不适用>`

## 0. 负责人决策摘要

用产品和交付语言说明本设计，不要求负责人先理解实现细节。

- 准备改变什么：`<待确认>`
- 为什么现在需要改变：`<待确认>`
- 推荐方案及一句话理由：`<待确认>`
- 对用户／业务结果的影响：`<待确认或无>`
- 对周期、成本、部署和长期维护的影响：`<待确认或无>`
- 需要负责人决定：`<明确问题与建议，或无>`
- 不实施／延后实施的结果：`<待确认>`

## 1. 背景、目标与边界

- 关联需求／Task：`<REQ-xxxx / TASK-NNNN>`
- 要实现或改变的技术结果：`<待确认>`
- 当前系统与问题：`<待确认>`
- 本设计包含：`<待确认>`
- 明确不做：`<待确认>`
- 约束与关键假设：`<待确认>`

## 2. 运行与交付上下文

- 运行环境和支持版本：`<待确认>`
- 主要语言、框架和依赖基线：`<待确认>`
- 构建、运行和正式入口：`<待确认>`
- 仓库／worktree／发布单元：`<待确认>`
- 现有能力、legacy 与复用边界：`<待确认>`

## 3. 方案与选择依据

| 候选 | 契合度 | 维护／安全／许可 | 迁移与退出 | 结论 |
| --- | --- | --- | --- | --- |
| `<方案>` | `<待确认>` | `<待确认>` | `<待确认>` | `<采用／拒绝及原因>` |

采用方案摘要：`<待确认>`

## 4. 模块职责与依赖

| 模块／组件 | 高内聚职责 | 代码路径 | 公开接口 | 数据所有权 | 允许依赖 | 禁止依赖 |
| --- | --- | --- | --- | --- | --- | --- |
| `<模块>` | `<单一变化原因>` | `<路径>` | `<API／事件／函数>` | `<表／schema／文件／无>` | `<模块>` | `<模块或层>` |

依赖方向：

```text
<例如：Interface -> Application -> Domain <- Infrastructure>
```

## 5. 接口、数据与状态

- API／事件／文件／schema 契约：`<待确认>`
- 核心数据模型与 owner：`<待确认>`
- 状态机、业务不变量和非法转换：`<待确认>`
- 兼容、版本化和消费者影响：`<待确认>`
- 数据迁移、回填、验证和回滚：`<待确认或不适用>`

## 6. 副作用、事务与失败语义

| 入口 | 数据／外部副作用 | 事务／幂等 | 超时／重试／取消 | 失败、补偿与恢复 |
| --- | --- | --- | --- | --- |
| `<入口>` | `<数据库／网络／文件／消息>` | `<待确认>` | `<待确认>` | `<待确认>` |

## 7. 质量属性

只保留会影响本设计的质量属性，不为填表复制通用类别。目标不能只写“高性能／高可用”，应说明作用范围、业务影响和可观察标准；无法验证时标为假设或未决。

| 属性／范围 | 业务影响与可测目标 | 验证方法／证据 | Owner、约束与依赖 | 已接受取舍 |
| --- | --- | --- | --- | --- |
| `<属性及组件／流程>` | `<影响；指标、阈值或可判定结果>` | `<测试／基准／演练／检查>` | `<owner；预算／环境／外部依赖>` | `<与其他属性的冲突及决定>` |

## 8. 实现与迁移计划

| 阶段 | 范围 | 兼容／迁移策略 | 验证 | 回滚／停止条件 |
| --- | --- | --- | --- | --- |
| 1 | `<待确认>` | `<待确认>` | `<待确认>` | `<待确认>` |

执行进度写入项目声明的 Durable Task State Owner；采用 BuildOS 新项目默认实例时使用关联的 `governance/tasks/TASK-<NNNN>-<slug>.md`。本文件只维护批准后的技术设计事实。

## 9. 测试与完成证据

- 单元／模块测试：`<待确认>`
- 契约／集成测试：`<待确认>`
- 端到端／真实流程：`<待确认>`
- 架构／依赖检查：`<待确认>`
- 统一质量命令：`<待确认>`
- 产品验收与发布证据入口：`<路径或不适用>`

## 10. 风险、技术债与未决决策

| 项目 | 类型 | 影响 | Owner | 决定／收口条件 |
| --- | --- | --- | --- | --- |
| `<待确认>` | `<风险／债务／未决>` | `<待确认>` | `<待确认>` | `<待确认>` |

需要长期记录的技术债进入 `engineering/TECH_DEBT.md`；影响模块、公共接口、schema、基础依赖或发布边界的长期决定按项目规则建立 ADR。
