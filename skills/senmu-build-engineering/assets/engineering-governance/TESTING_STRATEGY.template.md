# {{PROJECT_NAME}} 测试与质量验证策略

> Professional Name：Testing and Quality Validation Strategy
> 文档状态：初始化草案
> Owner：`<待确认>`
> 适用范围／发布单元：`<待确认>`
> 最近校准：{{DATE}}

## 1. 质量目标与风险

- 关联需求、架构和发布单元：`<待确认>`
- 核心业务不变量：`<待确认>`
- 高风险路径：`<权限／数据／支付／迁移／外部服务等>`
- 支持环境、版本和兼容边界：`<待确认>`
- 本策略不覆盖：`<待确认或无>`

## 2. 测试层级与 owner

| 层级 | 目录／入口 | 主要证明 | 真实依赖／替身边界 | 阻断级别 |
| --- | --- | --- | --- | --- |
| 单元 | `<路径>` | `<规则和边界>` | `<无或说明>` | `<G1-G4>` |
| 模块／契约 | `<路径>` | `<公开接口和 schema>` | `<待确认>` | `<G1-G4>` |
| 集成 | `<路径>` | `<数据库／队列／第三方协作>` | `<待确认>` | `<G1-G4>` |
| 端到端／真实流程 | `<入口>` | `<用户或系统主链>` | `<环境>` | `<G1-G4>` |

不适用层级写明原因，不为形式创建空测试目录。

## 3. 风险覆盖矩阵

| 风险／行为 | 正常路径 | 边界／错误 | 并发／幂等／事务 | 权限／数据 | 测试与证据 |
| --- | --- | --- | --- | --- | --- |
| `<行为>` | `<覆盖>` | `<覆盖>` | `<覆盖或不适用>` | `<覆盖或不适用>` | `<测试 ID／路径>` |

## 4. 测试数据与环境

- 数据来源与构造：`<待确认>`
- 隐私、脱敏和禁止使用的数据：`<待确认>`
- 隔离、并行和清理：`<待确认>`
- 时间、随机数、外部服务和网络固定方式：`<待确认>`
- 真实凭据、付费调用或外部写入的授权边界：`<待确认或不适用>`

## 5. 非功能验证

| 领域 | 目标／阈值 | 环境与方法 | 证据 |
| --- | --- | --- | --- |
| 性能与容量 | `<待确认或不适用>` | `<待确认>` | `<待确认>` |
| 可靠性与恢复 | `<待确认或不适用>` | `<待确认>` | `<待确认>` |
| 安全与权限 | `<待确认或不适用>` | `<待确认>` | `<待确认>` |
| 可访问性 | `<待确认或不适用>` | `<待确认>` | `<待确认>` |
| 可观测性 | `<待确认或不适用>` | `<待确认>` | `<待确认>` |

## 6. 统一质量命令

| 目的 | 命令／CI job | 适用范围 | 预计成本 |
| --- | --- | --- | --- |
| 自动格式化 | `<命令>` | `<范围>` | `<待确认>` |
| 快速检查 | `<命令>` | `<范围>` | `<待确认>` |
| 完整质量 | `<命令>` | `<范围>` | `<待确认>` |
| 架构／契约 | `<命令或评审入口>` | `<范围>` | `<待确认>` |
| 真实流程 | `<命令或操作入口>` | `<环境>` | `<待确认>` |

本地、提交前检查和 CI 尽量复用同一底层配置。

## 7. Flaky、跳过与例外

| 测试／规则 | 状态 | 原因与风险 | Owner | 恢复／收口条件 |
| --- | --- | --- | --- | --- |
| `<对象>` | `<flaky／skipped／excepted>` | `<待确认>` | `<待确认>` | `<待确认>` |

不得通过无限重跑、降低断言或删除测试伪造绿色结果。

## 8. 变更与发布门禁

- 缺陷修复回归要求：`<待确认>`
- 公共接口／schema 变化要求：`<待确认>`
- 数据迁移与回滚要求：`<待确认>`
- G3-G4 发布阻断项：`<待确认>`
- 产品验收入口：`<路径或不适用>`
- 发布后验证与回滚入口：`<路径或不适用>`

测试通过不等于产品验收或真实发布完成；最终状态分别读取 Product 和 Delivery 权威证据。
