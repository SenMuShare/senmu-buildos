# 项目架构契约

> 文档状态：现行
> 适用发布单元：`<发布单元>`
> 适用版本：`<版本或未版本化>`
> 最近校准：`<日期和 commit/tag/运行依据>`

## 1. 系统边界

- 系统职责：`<本系统负责什么>`
- 明确不负责：`<不属于本系统的能力>`
- 权威项目根：`<路径或仓库说明>`
- 正式运行／交付入口：`<入口>`

## 2. 模块登记表

| 模块 | 职责 | 代码路径 | 公开接口 | 数据所有权 | 允许依赖 | 禁止依赖 | 发布单元 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `<模块>` | `<单一职责>` | `<路径>` | `<API/事件/函数>` | `<表/schema/文件/无>` | `<模块>` | `<模块或层>` | `<发布单元>` |

## 3. 依赖方向

```text
<例如：UI -> Application -> Domain <- Infrastructure>
```

- 禁止跨层访问：`<规则>`
- 共享代码边界：`<规则>`
- 循环依赖策略：`<检查命令或人工门禁>`

## 4. 核心业务不变量

| 不变量 | 实现边界 | 保护测试 | 失败处理 |
| --- | --- | --- | --- |
| `<必须始终成立的规则>` | `<模块/数据库/服务>` | `<测试>` | `<行为>` |

## 5. 副作用与数据流

| 入口 | 业务处理 | 数据／外部副作用 | 事务／幂等 | 失败与补偿 |
| --- | --- | --- | --- | --- |
| `<入口>` | `<模块>` | `<数据库/网络/文件/消息>` | `<规则>` | `<规则>` |

## 6. 公开契约与兼容

- 公共 API／事件／schema：`<位置>`
- 兼容策略：`<向后兼容、版本化、迁移窗口>`
- 数据迁移与回滚：`<入口>`

## 7. 运行与发布边界

| 发布单元 | 构建 | 部署 | VERSION | CHANGELOG | 回滚 |
| --- | --- | --- | --- | --- | --- |
| `<单元>` | `<命令>` | `<入口>` | `<文件>` | `<文件>` | `<依据>` |

## 8. Legacy 边界

| 旧入口／实现 | 当前用途 | 禁止用途 | 移除触发条件 | 债务 ID |
| --- | --- | --- | --- | --- |
| `<路径>` | `<兼容/回滚/历史参考>` | `<不能作为新代码样板>` | `<条件>` | `<TD-ID>` |

能力替代时按同一变更单元核对状态、入口、事件／过滤链、消费者、测试和文档；暂留项在本表登记替代对象、owner 与退出条件。

## 9. 架构检查

- 快速架构检查：`<命令>`
- 完整架构检查：`<命令>`
- 只能语义评审的边界：`<说明>`
- 当前豁免：`<规则 ID、范围、原因、收口条件>`

## 10. 架构变更触发条件

出现新模块、公共接口、数据库 schema、依赖方向、第三方基础依赖、跨发布单元调用或 legacy 替换时，先更新本契约，并按项目规则决定是否新增 ADR。
