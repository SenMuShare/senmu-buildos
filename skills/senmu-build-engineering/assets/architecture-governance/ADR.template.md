# ADR-<NNNN>：<架构决策名称>

> Professional Name：Architecture Decision Record
> 状态：`proposed | accepted | superseded | deprecated`
> 决策日期：`<YYYY-MM-DD>`
> Owner／决策人：`<待确认>`
> 适用范围／发布单元：`<待确认>`
> 关联需求、Task、TD 或 Experiment：`<路径或 ID>`
> 替代／被替代 ADR：`<路径或不适用>`

本文件只记录会长期影响模块职责、依赖方向、公共契约、数据所有权、基础设施或发布边界的决定。普通实现细节留在 TD 或代码中。

## 决策摘要

- 最终决定：`<待确认>`
- 一句话理由：`<待确认>`
- 对产品、交付、成本和维护的影响：`<待确认或无>`

## 背景与约束

- 要解决的问题：`<待确认>`
- 当前事实与约束：`<待确认>`
- 不属于本决策的范围：`<待确认>`

## 候选方案

| 候选 | 优点 | 代价／风险 | 迁移与退出 | 结论 |
| --- | --- | --- | --- | --- |
| `<方案>` | `<待确认>` | `<待确认>` | `<待确认>` | `<采用／拒绝及原因>` |

## 决定与后果

- 采用方案：`<待确认>`
- 必须遵守的架构／契约边界：`<待确认>`
- 正向后果：`<待确认>`
- 接受的负向后果与技术债：`<待确认或无>`
- 实施、迁移、验证和回滚要求：`<待确认>`

## 重新评估与替代

- 重新评估触发条件：`<需求、规模、成本、风险或技术条件>`
- 替代本决策的方式：创建新 ADR，引用本文件并将本文件标记为 `superseded`；不得静默改写历史决定。
