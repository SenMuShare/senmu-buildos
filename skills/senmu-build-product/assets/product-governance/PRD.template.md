# {{PROJECT_NAME}} 产品需求文档

> Professional Name：Product Requirements Document
> 文档状态：初始化草案
> Owner：`<待确认>`
> 适用产品线／范围：`<待确认>`
> 最近校准：{{DATE}}

## 1. 背景与目标

- 要解决的问题／机会：`<待确认>`
- 目标用户与核心场景：`<待确认>`
- 期望结果：`<待确认>`
- 成功指标及测量方式：`<指标、基线、目标、时间窗口和证据；不适用时说明成功判断>`

## 2. 范围

- 本期包含：`<待确认>`
- 明确不做：`<待确认>`
- 相关需求 ID：`<REQ-xxxx>`
- 适用阶段／迭代：`<待确认>`

## 3. 用户、角色与场景

| 用户／角色 | 目标 | 入口／触发 | 完成结果 |
| --- | --- | --- | --- |
| `<角色>` | `<目标>` | `<入口>` | `<可观察结果>` |

复杂产品可以把核心场景拆成可独立验证的价值切片；一次性交付、工作流、媒体和 POC 分别使用触发／输入／结果、成品接受条件或证据目标表达，不强制伪装成软件用户故事。

## 4. 产品需求与业务规则

| Requirement ID | 行为／规则 | 优先级或承诺 | 验收索引 |
| --- | --- | --- | --- |
| REQ-0001 | `<待确认>` | `<待确认>` | AC-01 |

## 5. 流程、状态与异常

- 核心流程：`<待确认>`
- 关键状态及转换：`<待确认或不适用>`
- 失败、空状态、重试和恢复：`<待确认>`
- 权限、计费、数据归属、隐私与合规：`<待确认或不适用>`

## 6. 依赖、假设与约束

- 外部依赖：`<待确认或无>`
- 关键假设及验证方式：`<待确认或无>`
- 时间、成本、兼容或运营约束：`<待确认或无>`

## 7. 验收标准

| AC ID | 前置条件 | 操作／事件 | 预期结果 | 证据 |
| --- | --- | --- | --- | --- |
| AC-01 | `<待确认>` | `<待确认>` | `<可观察结果>` | `<验收时填写>` |

## 8. 未决问题与决策

| 问题／决定 | Owner | 截止／日期 | 结果或影响 |
| --- | --- | --- | --- |
| `<待确认>` | `<待确认>` | `<待确认>` | `<待确认>` |

## 9. 变更记录

| 日期 | 变更 | 影响的 REQ／迭代 | 确认者 |
| --- | --- | --- | --- |
| {{DATE}} | 初始化 | `<待确认>` | `<待确认>` |
