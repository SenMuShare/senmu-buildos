# Repository and Release Unit Register

> 文档状态：初始化草案  
> 最近校准：{{DATE}}

仅在项目存在多个仓库或多个可独立构建、部署、回滚的发布单元时使用。单一发布单元项目不为形式完整而创建本表。

| Release Unit | 产品／职责 | 仓库／权威路径 | 版本／Changelog | Tag 语义／前缀 | 构建／制品 | 目标环境／部署范围 | 生产验证 | 回滚入口 | 联动依赖 | 明确不涉及 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `<待确认>` | `<待确认>` | `<待确认>` | `<待确认>` | `<待确认>` | `<待确认>` | `<待确认>` | `<待确认>` | `<待确认>` | `<待确认>` | `<待确认>` |

## 跨单元契约

- API／事件／文件／共享包契约：`<待确认>`
- 兼容与联动发布规则：`<待确认>`
- 路径级 CI／CODEOWNERS／权限：`<待确认>`
- 旧仓库、legacy 和迁移窗口：`<待确认或不适用>`
