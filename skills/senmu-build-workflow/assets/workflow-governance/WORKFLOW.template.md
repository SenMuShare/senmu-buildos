# {{PROJECT_NAME}} 工作流规范

> 文档状态：初始化草案  
> 最近校准：{{DATE}}

## 工作流契约

- Workflow ID／版本：`<待确认>`
- 目标结果：`<待确认>`
- 触发方式与适用边界：`<待确认>`
- 唯一公开入口：`<待确认>`
- 输入 schema/config 与前置条件：`<待确认>`
- 输出 schema 与下游消费者：`<待确认>`
- 运行 ID 规则：`<待确认>`
- Run Manifest／唯一可写状态源：`<待确认>`
- 幂等键与外部副作用：`<待确认>`
- 超时、取消、断点续作与重试边界：`<待确认>`
- 运行完成条件与失败状态：`<待确认>`
- 权限、人工确认和停止点：`<待确认>`

## 步骤与恢复边界

| Step ID | 输入 | 输出 | 验证 | 可重试／幂等 | 失败与恢复入口 |
|---|---|---|---|---|---|
| `<step-01>` | `<待确认>` | `<待确认>` | `<待确认>` | `<待确认>` | `<待确认>` |

> 工作流契约不登记某次运行的当前 step。动态状态进入 Run Manifest 或项目既有运行状态系统；跨阶段任务计划与进度进入 Task Plan & Status Record。

## 目录角色映射

| 角色 | 项目真实路径 | Git/存储规则 | 保留/清理规则 |
|---|---|---|---|
| 源输入 | `<待确认>` | `<待确认>` | `<待确认>` |
| 工作区/staging | `<待确认>` | `<待确认>` | `<待确认>` |
| 临时/缓存 | `<待确认>` | `<待确认>` | `<待确认>` |
| 运行状态/Run Manifest | `<待确认>` | `<待确认>` | `<待确认>` |
| 最终交付物 | `<待确认>` | `<待确认>` | `<待确认>` |
| 证据/回执 | `<待确认>` | `<待确认>` | `<待确认>` |
| 归档 | `<待确认>` | `<待确认>` | `<待确认>` |

## 验收与交付

- 质量/验收入口：`<待确认>`
- 最终交付位置和命名：`<待确认>`
- 交付 ID/版本规则：`<待确认>`
- 发布状态（如适用）：`<待确认或不适用>`
- 回执、校验和与恢复入口：`<待确认>`
- legacy/旧产物边界：`<待确认>`

## 状态责任分离

- Task Plan & Status Record：`<路径／外部对象 ID／不适用>`
- Workflow Contract：`<路径>`
- Run State Owner：`<路径、表、队列或外部系统>`
- Delivery／Publication Owner：`<路径或外部系统／不适用>`
